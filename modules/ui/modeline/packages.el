;;; modules/ui/modeline/packages.el -*- lexical-binding: t; no-byte-compile: t; -*-

(straight-use-package 'doom-modeline)

;; nerd-icons provides the glyphs doom-modeline shows for major modes,
;; buffer state, vcs, etc. Run `M-x nerd-icons-install-fonts' once on a
;; new machine to get the actual font files.
(straight-use-package 'nerd-icons)
