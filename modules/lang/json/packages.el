;;; modules/lang/json/packages.el -*- lexical-binding: t; no-byte-compile: t; -*-

;; `json-ts-mode' (tree-sitter) and `js-json-mode' (legacy) both ship
;; built-in to Emacs 29+. No external mode pkg needed.
;; LSP via `vscode-json-language-server' (install via `M-x lsp-install
;; -server json-ls' or `npm i -g vscode-langservers-extracted').
