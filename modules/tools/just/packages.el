;;; modules/tools/just/packages.el -*- lexical-binding: t; no-byte-compile: t; -*-

;; just-mode (leon-barrett): major mode for editing Justfiles
;; (the command runner from https://github.com/casey/just).
;; Provides syntax highlighting, indentation, and comment support.
(straight-use-package 'just-mode)
